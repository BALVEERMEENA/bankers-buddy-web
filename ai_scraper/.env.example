# Copy to .env and fill in your key (or use `ant auth login` instead).
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the model used for extraction.
# Default is claude-opus-5. For a cheaper run:
# BANKERS_MODEL=claude-haiku-4-5
